<%= form_with(model: [@quiz_question.quiz, @quiz_question]) do |f| %>
  <% if @quiz_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quiz_question.errors.count, "error") %> prohibited this quiz_question from being saved:</h2>

      <ul>
      <% @quiz_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br />
    <%= f.text_field :question %>
  </div>
  <div class="field">
    <%= f.label :quiz %><br />
    <%= f.text_field :quiz %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
